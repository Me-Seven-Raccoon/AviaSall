import React from 'react';

import classes from './NoFilter.scss';

const NoFilter = () => {
  return (
    <div className={classes.boxNoFilter}>
      <p> Рейсов, не найдено</p>
    </div>
  );
};

export default NoFilter;
